import { styled } from "../../stitches.config";
export var LogoStyled = styled("div", {
    display: "inline-flex",
    verticalAlign: "middle",
    margin: "0",
    padding: "0",
    svg: {
        height: "auto",
        width: "auto"
    }
});
