import { styled } from '../../stitches.config';

export const DatesStyled = styled('div', {
  button: {
    fontSize: '$small',
    lineHeight: 1,
  },
  maxWidth: '30rem',
  padding: '$4',

  width: '100%',
});

export default DatesStyled;
