import { styled } from '../../stitches.config';

export const ImageStyled = styled('div', {
  height: '100%',
  position: 'relative',
  transition: '$default',
  width: '100%',
});

export default ImageStyled;
