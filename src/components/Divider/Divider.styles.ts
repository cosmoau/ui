/** @format */

import { styled } from '../../stitches.config';

export const DividerStyled = styled('div', {
  height: '0.1rem',
  margin: '0 auto',
  maxWidth: '100%',
  width: '100%',
});

export default DividerStyled;
